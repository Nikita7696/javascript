
for (let index = 0; index <=5 ; index++) { // for breaking loop
 if (index==2) {
    break;
 }
    console.log(index);
}

console.log(`===========================`);

for (let index = 0; index <=5 ; index++) { // for Continue loop
    if (index==2) {
       continue;
    }
       console.log(index);
   }